program lab2;

{$APPTYPE CONSOLE}
uses
  SysUtils;

Var x, y:Single;
Begin
        WriteLn('Write x, y: ');
  	ReadLn(x, y);
  	if ((x+1)*(x+1)+y*y <=1) and (y<=x+1) and (y>=-x-1) and (x<=0) then
    begin
        WriteLn('inside');
    end
        else
      WriteLn('outside');
 	ReadLn;
end.
